<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Đường dẫn lưu log - sẽ mount ra ngoài -->
    <property name="LOG_PATH" value="/app/logs" />
    <property name="LOG_FILE" value="${LOG_PATH}/company-service.log" />

    <!-- Ghi ra file -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/company-service.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Vẫn giữ log ra console để docker logs vẫn thấy -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Log level cho Kafka -->
    <logger name="org.springframework.kafka" level="DEBUG"/>
    <logger name="org.apache.kafka" level="DEBUG"/>
    <logger name="com.tihuz" level="DEBUG"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>